# Defines the actions that an operator can take
# Each string can be included in the operator's "Action_permissions" field
# (a string containing a comma-separated list) in the database.
CREATE = 'create'
# DROP = 'drop'
READ = 'read'
INSERT = 'insert'
UPDATE = 'update'
DELETE = 'delete'
